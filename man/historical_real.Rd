% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical_real.R
\name{historical_real}
\alias{historical_real}
\title{Convert a value to its historical equivalent using a GDP deflator}
\usage{
historical_real(amount, to_year, currency, from_year = NULL, round = 2)
}
\arguments{
\item{amount}{Numeric (scalar or vector). The monetary amount(s) in the
reference year.}

\item{to_year}{Integer. The historical year to convert back to.}

\item{currency}{Character. Currency code (\code{"GBP"}, \code{"AUD"}, \code{"USD"},
\code{"EUR"}, \code{"CAD"}, \code{"JPY"}, \code{"CNY"}, \code{"CHF"}, \code{"NZD"}, \code{"INR"}, \code{"KRW"},
\code{"BRL"}, \code{"NOK"}) or country name
(\code{"Australia"}, \code{"United States"}, etc.) — case-insensitive.}

\item{from_year}{Integer. The year the amount is from. Defaults to the
latest year available in the deflator series.}

\item{round}{Integer or \code{NULL}. Number of decimal places to round to
(default 2). Use \code{NULL} for full precision.}
}
\value{
A numeric value (or vector) representing the historical equivalent
amount.
}
\description{
Takes a monetary amount from a recent year and returns what it would have
been worth in a specified historical year, using GDP deflator data sourced
from the World Bank Development Indicators (indicator: NY.GDP.DEFL.ZS).
This is the inverse of \code{\link{adjust_real}}.
}
\details{
For converting consumer or personal values, use
\code{\link{historical_value}} which uses CPI instead.
}
\examples{
# What would UK GDP of £2 trillion today have been in 1990 terms?
historical_real(2e12, 1990, "GBP")

# What would USD 1 trillion in 2020 have been worth in 2000?
historical_real(1e12, 2000, "USD", from_year = 2020)

}
\seealso{
Other GDP deflator adjustment: 
\code{\link{adjust_real}()}
}
\concept{GDP deflator adjustment}
